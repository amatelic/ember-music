import { musicTimer } from 'music/helpers/music-timer';
import { module, test } from 'qunit';

module('Unit | Helper | music timer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = musicTimer([42]);
  assert.ok(result);
});
